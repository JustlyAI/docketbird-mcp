.git
.github
.gitignore
.env
.env.example

# SQLite auth database — never bake user data (emails, password hashes, API
# keys) into the image. CI builds from a clean checkout, but a local
# `docker build .` would otherwise copy a developer's data/ into the image.
data/
*.db

__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
env/
ENV/
.DS_Store
.cursorignore
.cursorrules
build/
docketbird_mcp.egg-info/

# Project docs and config (not needed in container)
.claude/
CLAUDE.md
README.md
LICENSE
*.pdf
*.md
plans/
tests/
.dmux/
.dmux-hooks/
agents/
